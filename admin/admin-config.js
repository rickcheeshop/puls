/* Rick Chee Shop Admin — Firebase Spark direct mode.
   No static ADMIN_API_KEY is stored in GitHub.
   Firebase Authentication identifies the Admin and Firestore Rules enforce access.
*/
window.RickCheeAdminConfig = {
  authProvider: 'firebase-auth',
  dataProvider: 'firebase-firestore-direct'
};
